import React, { Component } from 'react';
import styles from './BlogView.scss';
import classNames from 'classnames/bind';

const cx = classNames.bind(styles);

class BlogView extends Component {
  render() {
    return (
      <div>
        BlogView
      </div>
    );
  }
}

export default BlogView;