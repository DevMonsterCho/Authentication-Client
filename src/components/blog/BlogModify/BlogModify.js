import React, { Component } from 'react';
import styles from './BlogModify.scss';
import classNames from 'classnames/bind';

const cx = classNames.bind(styles);

class BlogModify extends Component {
  render() {
    return (
      <div>
        BlogModify
      </div>
    );
  }
}

export default BlogModify;